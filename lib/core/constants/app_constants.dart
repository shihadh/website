import 'package:flutter/material.dart';

class AppConstants {
  static const double horizontalPadding = 80.0;
  static const double mobilePadding = 24.0;

  static const Duration scrollAnimationDuration = Duration(milliseconds: 1000);
  static const Curve scrollCurve = Curves.easeOutCubic;

  static const double phoneAspectRatio = 9 / 19.5;
  static const double phoneBorderRadius = 40.0;
}
